// Временная заглушка для proposal_widget.dart
// Этот файл содержит много ошибок и требует серьезной переработки
// Пока что отключаем его, чтобы сосредоточиться на основных экранах

import 'package:flutter/material.dart';

class ProposalWidget extends StatelessWidget {
  const ProposalWidget({super.key});

  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('ProposalWidget временно отключен'));
}
