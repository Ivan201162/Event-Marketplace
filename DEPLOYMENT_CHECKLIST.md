# Чек-лист деплоя ULTIMATE CLEAN v4.4.0+6

## ✅ Предварительная проверка

- [x] Все изменения закоммичены в ветку `prod/ultimate-clean-max`
- [x] APK собран: `build/app/outputs/flutter-apk/app-release.apk` (77.4 MB)
- [x] Версия обновлена: `4.4.0+6` (build: `v4.4-max-profile`)
- [x] Отчёты созданы: `ULTIMATE_CLEAN_REPORT.md`, `ULTIMATE_SUMMARY.md`

---

## 🔥 Firebase Deploy

### 1. Firestore Indexes

```bash
firebase deploy --only firestore:indexes
```

**Проверка:**
- [ ] Индексы успешно развёрнуты
- [ ] Нет ошибок в консоли Firebase
- [ ] Индексы видны в Firebase Console → Firestore → Indexes

**Добавленные индексы:**
- `specialists`: `(role ASC, city ASC, categories ARRAY, rating DESC)`
- `specialists`: `(role ASC, categories ARRAY, minPrice ASC, rating DESC)`
- `users`: `(firstNameLower ASC, lastNameLower ASC)`
- `users`: `(usernameLower ASC, role ASC)`

### 2. Firestore Rules (опционально)

```bash
firebase deploy --only firestore:rules
```

**Проверка:**
- [ ] Правила развёрнуты
- [ ] Правила для `requests` корректны (создание разрешено авторизованным)

### 3. Storage Rules (опционально)

```bash
firebase deploy --only storage:rules
```

**Проверка:**
- [ ] Правила развёрнуты
- [ ] Правила для `uploads/requests/{userId}/{requestId}/*` корректны

---

## 📱 Установка APK на устройство

### 1. Подключение устройства

```bash
adb devices
```

**Проверка:**
- [ ] Устройство подключено и видно в списке
- [ ] Статус: `device` (не `unauthorized`)

### 2. Удаление старой версии (опционально)

```bash
adb uninstall com.eventmarketplace.app
```

**Или:**
```bash
adb uninstall com.eventmarketplace.app || true
```

### 3. Установка новой версии

```bash
adb install -r build/app/outputs/flutter-apk/app-release.apk
```

**Проверка:**
- [ ] Установка успешна (Success)
- [ ] Нет ошибок в консоли

### 4. Запуск приложения

```bash
adb shell monkey -p com.eventmarketplace.app -c android.intent.category.LAUNCHER 1
```

**Проверка:**
- [ ] Приложение запустилось
- [ ] Нет крэшей при запуске

---

## 📊 Проверка логов (logcat)

### Запуск мониторинга логов

```bash
adb logcat -s flutter:I | Select-String "APP:|HOME_|SEARCH_|PROFILE_|REVIEWS_|REQUEST_|ERROR:"
```

### Ожидаемые маркеры

- [ ] `APP: BUILD OK v4.4-max-profile` - при запуске
- [ ] `HOME_LOADED` - при открытии главной
- [ ] `HOME_BANNER_RENDERED` - при отображении плашки
- [ ] `SEARCH_OPENED` - при открытии поиска
- [ ] `SEARCH_RESULT_COUNT:{n}` - при получении результатов
- [ ] `PROFILE_OPENED:{uid}` - при открытии профиля
- [ ] `REVIEWS_LOADED:{count}` - при загрузке отзывов
- [ ] `REQUEST_CREATE_OPENED` - при открытии создания заявки
- [ ] `REQUEST_PUBLISHED:{id}` - при публикации заявки

---

## ✅ Функциональная проверка

### Главная страница

- [ ] Плашка пользователя отображается: **Имя Фамилия** (крупно, жирно)
- [ ] Ниже: **@username** (меньше, если есть)
- [ ] Ещё ниже: **город** (или «Город не указан»)
- [ ] Нет красного бейджа "PATCH-TEST-***"
- [ ] Тап по плашке → открывается профиль (`/profile/me`)
- [ ] Секция "Лучшие специалисты недели — Россия" отображается
- [ ] Секция "Лучшие специалисты недели — {город}" отображается (если город указан)
- [ ] Карточки специалистов кликабельны → открывают профиль

### Поиск

- [ ] Экран поиска открывается без ошибок
- [ ] Кнопка "Попробовать снова" реально перезапускает запрос (показывает спиннер)
- [ ] Фильтры: город, категории (multi-select), цена, рейтинг, опыт, формат
- [ ] Кнопки "Сбросить" и "Сохранить фильтр" работают
- [ ] Back-кнопка возвращает на предыдущий экран

### Профиль

- [ ] Профиль открывается без ошибок
- [ ] Вкладки-иконки сверху: Посты, Рилсы, Отзывы
- [ ] Тень между шапкой и вкладками видна
- [ ] Отзывы отображаются в больших карточках
- [ ] Карточки отзывов: аватар, имя, рейтинг, текст, фото, дата
- [ ] Back-кнопка возвращает на предыдущий экран
- [ ] Кнопка "Редактировать профиль" работает (если свой профиль)

### Создание заявки

- [ ] Экран открывается без ошибок
- [ ] Типы мероприятий: расширенный список (33+ типа), + "Другое"
- [ ] Публикация заявки: toast/snackbar + возврат на список
- [ ] Нет ошибки "permission denied"
- [ ] Back-кнопка возвращает на предыдущий экран

### Back-навигация

- [ ] Профиль: Back → предыдущий экран
- [ ] Поиск: Back → предыдущий экран
- [ ] Создание заявки: Back → предыдущий экран
- [ ] Никакие экраны не закрывают приложение при нажатии Back

---

## 🐛 Проверка ошибок

### Firestore

- [ ] Нет ошибок `failed-precondition` в поиске
- [ ] Нет ошибок `permission-denied` при создании заявки
- [ ] Все запросы выполняются успешно

### UI

- [ ] Нет красных экранов ошибок
- [ ] Нет белых экранов (зависаний)
- [ ] Все экраны загружаются корректно

---

## 📝 Финальная проверка

- [ ] Все маркеры из logcat найдены
- [ ] Все функциональные проверки пройдены
- [ ] Нет критических ошибок
- [ ] APK работает стабильно

---

## 🎯 Готовность к продакшену

**Статус:** ✅ Готово к тестированию

После успешного прохождения всех проверок:
1. ✅ Можно делать merge в main/master
2. ✅ Можно публиковать в Google Play
3. ✅ Можно запускать мониторинг

---

**Дата:** 2024-11-04  
**Версия:** 4.4.0+6  
**Build:** v4.4-max-profile







