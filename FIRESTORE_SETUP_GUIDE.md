# Руководство по настройке Firebase Firestore

## Обзор

Это руководство описывает полную настройку Firebase Firestore для проекта Event Marketplace, включая исправление проблем с правами доступа и настройку тестовых данных.

## Проблемы, которые были исправлены

### 1. Ошибка permission-denied
**Проблема**: `[cloud_firestore/permission-denied] The caller does not have permission to execute the specified operation.`

**Решение**: 
- Обновлены Firestore Security Rules для корректной работы с коллекциями
- Изменена коллекция с `ideas` на `posts` в правилах
- Добавлены правила для новых коллекций: `categories`, `tariffs`, `plans`

### 2. Несоответствие коллекций
**Проблема**: Правила были настроены для коллекции `ideas`, но код использовал `posts`

**Решение**: Обновлены правила для работы с коллекцией `posts`

## Настройка Firestore Security Rules

### Обновленные правила доступа

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Публичное чтение постов (лента). Запись только аутентифицированным.
    match /posts/{postId} {
      allow read: if true;
      allow create: if request.auth != null && 
        request.auth.uid == resource.data.authorId;
      allow update: if request.auth != null && 
        (request.auth.uid == resource.data.authorId || 
         request.auth.uid in resource.data.moderators);
      allow delete: if request.auth != null && 
        request.auth.uid == resource.data.authorId;
    }

    // Специалисты — публичное чтение, запись только владельцу
    match /specialists/{specialistId} {
      allow read: if true;
      allow create: if request.auth != null && request.auth.uid == specialistId;
      allow update: if request.auth != null && request.auth.uid == specialistId;
      allow delete: if request.auth != null && request.auth.uid == specialistId;
    }

    // Категории — публичное чтение, запись только админам
    match /categories/{categoryId} {
      allow read: if true;
      allow write: if request.auth != null && 
        request.auth.token.admin == true;
    }

    // Тарифы/планы — публичное чтение, запись только админам
    match /tariffs/{tariffId} {
      allow read: if true;
      allow write: if request.auth != null && 
        request.auth.token.admin == true;
    }
  }
}
```

## Настройка коллекций

### Основные коллекции

1. **users** - Пользователи приложения
2. **specialists** - Специалисты (ведущие, фотографы, флористы и т.д.)
3. **categories** - Категории специалистов
4. **posts** - Посты в ленте
5. **chats** - Чаты между пользователями
6. **tariffs** - Тарифные планы
7. **bookings** - Заявки на услуги

### Права доступа

#### Авторизованные пользователи могут:
- ✅ Читать ленту постов
- ✅ Читать категории
- ✅ Читать специалистов
- ✅ Участвовать в чатах
- ✅ Создавать посты
- ✅ Создавать заявки

#### Неавторизованные пользователи могут:
- ✅ Читать ленту постов
- ✅ Читать категории
- ✅ Читать специалистов
- ❌ Участвовать в чатах
- ❌ Создавать посты
- ❌ Создавать заявки

## Тестовые данные

### Категории специалистов
- 🎤 Ведущие
- 📸 Фотографы
- 🌸 Флористы
- 🎵 Музыканты
- 🎨 Декораторы

### Тарифные планы
- **Базовый** (0 ₽) - Основные возможности
- **Профессиональный** (2990 ₽) - Расширенные возможности
- **Премиум** (5990 ₽) - Максимальные возможности

### Тестовые специалисты
Создаются специалисты с полными профилями, включая:
- Фотографии
- Описания
- Цены
- Рейтинги
- Портфолио

## Развертывание

### 1. Проверка конфигурации
```powershell
.\scripts\test_firestore_setup.ps1
```

### 2. Развертывание правил
```powershell
.\scripts\deploy_firestore_rules.ps1
```

### 3. Создание тестовых данных
В приложении вызовите:
```dart
final testDataService = TestDataService();
await testDataService.createAllTestData();
```

## Тестирование

### Автоматическое тестирование
```dart
final firestoreTestService = FirestoreTestService();
await firestoreTestService.runComprehensiveTest();
```

### Ручное тестирование
1. Запустите приложение
2. Проверьте загрузку ленты
3. Проверьте отображение категорий
4. Проверьте список специалистов
5. Проверьте авторизацию

## Индексы Firestore

Обновлены индексы для оптимизации запросов:
- Посты по дате создания
- Посты по автору
- Посты по тегам
- Посты по количеству лайков
- Специалисты по категории и рейтингу
- Чаты по участникам

## Мониторинг

### Логи Firestore
Проверьте логи в Firebase Console для выявления проблем с правами доступа.

### Тестирование прав доступа
Используйте `FirestoreTestService` для автоматического тестирования всех сценариев доступа.

## Устранение неполадок

### Ошибка permission-denied
1. Проверьте, что правила развернуты
2. Убедитесь, что пользователь авторизован
3. Проверьте соответствие коллекций в коде и правилах

### Медленная загрузка
1. Проверьте индексы Firestore
2. Оптимизируйте запросы
3. Используйте пагинацию

### Отсутствие данных
1. Проверьте создание тестовых данных
2. Убедитесь в корректности правил доступа
3. Проверьте логи ошибок

## Заключение

После выполнения всех шагов:
- ✅ Проблема permission-denied исправлена
- ✅ Настроены все необходимые коллекции
- ✅ Созданы тестовые данные
- ✅ Настроены права доступа для авторизованных и неавторизованных пользователей
- ✅ Приложение готово к тестированию

Теперь пользователи могут:
- Читать ленту без авторизации
- Видеть категории и специалистов
- Авторизоваться для полного функционала
- Создавать посты и участвовать в чатах
